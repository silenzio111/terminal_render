[package]
name = "mdx"
version = "0.1.0"
edition = "2021"
description = "Terminal Markdown renderer — renders .md files as PNG via Typst and the Kitty graphics protocol"

[dependencies]
base64 = "0.22"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
libc = "0.2"
tempfile = "3"